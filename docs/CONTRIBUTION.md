# Version ID
first digit is kept for stable releases  
second digits represent changes

# Commit Message
Commit Messages look like this:

```
versionID: module: submodule: breif explain
```

example with a `taskScheduler`...  
- init repo  
- configUtil: json file r/w  
- testing: timestamp calculation

# Documentation
markdown files, goes to `./docs/`, make sure have hyper link in repository description (`README.md`)